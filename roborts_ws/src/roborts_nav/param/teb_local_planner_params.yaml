TebLocalPlannerROS:
  odom_topic: odom
  map_frame: /map

  acc_lim_x: 3
  acc_lim_y: 3
  acc_lim_theta: 3
  max_vel_x: 16.0
  max_vel_x_backwards: 16.0
  max_vel_y: 16.0
  max_vel_theta: 16.0


  min_turning_radius: 0.0
  is_footprint_dynamic: false
  footprint_model: # types: "point", "circular", "two_circles", "line", "polygon"
   type: "polygon"  #point指定用于优化的机器人足迹模型类型。不同的类型是“点”，“圆形”，“线”，“two_circles”和“多边形”。模型的类型会显着影响所需的计算时间。
   radius: 0.2 # 0.2该参数仅与“圆形”类型相关。它包含圆的半径。圆心位于机器人的旋转轴上。
   line_start: [-0.2, 0.0] # [-0.3,0.0]此参数仅与“线”类型相关。它包含线段的起始坐标。
   line_end: [0.2, 0.0] # [0.3,0.0]此参数仅与“线”类型相关。它包含线段的结束坐标。
   front_offset: 0.15 # 0.2该参数仅与类型“ two_circles”相关。它描述了前圆的中心沿机器人的x轴偏移了多少。假定机器人的旋转轴位于[0,0]。
   front_radius: 0.15 # 0.2该参数仅与类型“ two_circles”相关。它包含前圆的半径。
   rear_offset: 0.1 # 0.2该参数仅与类型“ two_circles”相关。它描述了后圆心沿机器人的负x轴偏移了多少。假定机器人的旋转轴位于[0,0]。
   rear_radius: 0.15 # 0.2该参数仅与类型“ two_circles”相关。它包含后圆的半径。
   vertices: [ [-0.3, -0.3], [-0.3, 0.3], [0.3, 0.3], [0.3, -0.3] ] # 默认值：[[0.25，-0.05]，[...]，...],此参数仅与“多边形”类型相关。它包含多边形顶点的列表（每个2d坐标）。多边形始终是封闭的：不要在末端重复第一个顶点。


  yaw_goal_tolerance: 0.2
  xy_goal_tolerance: 0.2
  free_goal_vel: true

  dt_ref: 0.3
  dt_hysteresis: 0.1
  min_samples: 3 
  global_plan_overwrite_orientation: true
  global_plan_viapoint_sep: 0.1  #
  max_global_plan_lookahead_dist: 2.0 #考虑优化的全局计划子集的最大长度（累积欧几里得距离）
  force_reinit_new_goal_dist: 1.0
  feasibility_check_no_poses: 1    #   
  publish_feedback: false
  allow_init_with_backwards_motion: false
  exact_arc_length: false
  shrink_horizon_backup: true
  shrink_horizon_min_duration: 10.0

  min_obstacle_dist: 0.3 #避障距离(障碍物膨胀半径)
  include_costmap_obstacles: true
  costmap_obstacles_behind_robot_dist: 1.0
  inflation_dist: 0.4
  include_dynamic_obstacles: false
  legacy_obstacle_association: false
  obstacle_association_force_inclusion_factor: 0.5
  obstacle_association_cutoff_factor: 5
  costmap_converter_plugin: ""
  costmap_converter_spin_thread: true
  costmap_converter_rate: 5.0

  no_inner_iterations: 3
  no_outer_iterations: 3
  penalty_epsilon: 0.1
  weight_acc_lim_x: 1.0
  weight_acc_lim_theta: 17.0
  weight_max_vel_x: 3.0
  weight_max_vel_theta: 6.0
  weight_kinematics_nh: 300.0 #调整顺应纵向运动和非顺应横向运动（扫射）之间的权衡。
  weight_kinematics_forward_drive: 1000.0
  weight_kinematics_turning_radius: 0
  weight_optimaltime: 6 #这个参数是最优时间权重，如果大了，那么车会在直道上快速加速，并且路径规划的也会切内道
  weight_obstacle: 25.0
  weight_viapoint: 200.0  #
  weight_inflation: 0.05
  weight_adapt_factor: 2.0

  enable_homotopy_class_planning: false
  enable_multithreading: true
  max_number_classes: 2 #4 改 2 规划出的最多轨迹数,这个参数是可以解决多个障碍堵路规划抖动问题
  selection_cost_hysteresis: 1.0
  selection_obst_cost_scale: 100.0
  selection_viapoint_cost_scale: 1.0
  selection_alternative_time_cost: false
  roadmap_graph_no_samples: 15
  roadmap_graph_area_width: 5
  h_signature_prescaler: 1.0
  h_signature_threshold: 0.1
  obstacle_heading_threshold: 0.45
  visualize_hc_graph: false
  viapoints_all_candidates: true
  switching_blocking_period: 0.0
  wheelbase: 0.4



